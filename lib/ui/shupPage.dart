import 'package:flutter/material.dart';

class ShupPage extends StatelessWidget {
  const ShupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('ShupPage')),
    );
  }
}